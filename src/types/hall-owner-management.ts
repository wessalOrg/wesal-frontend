/**
 * Hall Owner Profile in the management workspace uses the shared UserProfile model
 * (GET/PUT /profile). Keep this alias for feature naming clarity.
 */
export type { UserProfile as HallOwnerManagementProfile } from "@/types/profile";
